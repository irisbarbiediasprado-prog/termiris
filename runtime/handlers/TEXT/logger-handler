#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail

ROOT="${TERMIRIS_RUNTIME:-$HOME/.termiris/runtime}"

mkdir -p "$ROOT/cache"

cat "$1" >> "$ROOT/cache/runtime.log"
printf '\n' >> "$ROOT/cache/runtime.log"
